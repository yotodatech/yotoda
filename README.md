# Yotoda

The studio site for **Yotoda** — https://yotoda.tech

A static Vite + React SPA. Deploy the `dist/` folder to Cloudflare Pages:

    wrangler pages deploy dist --project-name yotoda --branch main

See `LLM.md` for details.
